# Plantilla de referencia: NO aplicar este archivo tal cual ni commitear
# una copia con valores reales. Generar el Secret directo contra el
# clúster:
#
#   oc create secret generic taskflow-db-credentials \
#     --from-literal=username=taskflow \
#     --from-literal=password="$(openssl rand -base64 24)"
#
apiVersion: v1
kind: Secret
metadata:
  name: taskflow-db-credentials
type: Opaque
stringData:
  username: "TODO"
  password: "TODO"
