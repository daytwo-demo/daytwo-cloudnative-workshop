apiVersion: v1
kind: Service
metadata:
  name: taskflow-api
  labels:
    app: taskflow-api
spec:
  selector:
    app: taskflow-api
  ports:
    # SOLUCIÓN (lab01): puerto donde escucha Kestrel dentro del pod.
    - name: http
      port: 8080
      targetPort: 8080
