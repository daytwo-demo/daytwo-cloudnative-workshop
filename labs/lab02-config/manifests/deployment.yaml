apiVersion: apps/v1
kind: Deployment
metadata:
  name: taskflow-api
  labels:
    app: taskflow-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taskflow-api
  template:
    metadata:
      labels:
        app: taskflow-api
    spec:
      containers:
        - name: taskflow-api
          # SOLUCIÓN (lab02): imagen reconstruida en el paso 2 (tag lab02,
          # no lab01), ya sin sidecar de Postgres.
          image: "image-registry.openshift-image-registry.svc:5000/tu-namespace/taskflow-api:lab02"
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: taskflow-config
          env:
            - name: Db__Username
              valueFrom:
                secretKeyRef:
                  name: taskflow-db-credentials
                  key: username
            - name: Db__Password
              valueFrom:
                secretKeyRef:
                  name: taskflow-db-credentials
                  key: password
